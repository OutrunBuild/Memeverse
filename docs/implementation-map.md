# MemeverseV2 实现面映射（Implementation Map）

## 1. 映射原则

- 仅记录“当前源码已落地”表面（no roadmap guess）。
- 每个表面都给出：职责、权责边界、升级性、证据来源、实现状态。
- 当前规则真源是 `docs/spec/*.md`；`src/**` 与 `test/**` 是可验证证据面。
- 升级性规则主文档是 [docs/spec/upgradeability.md](spec/upgradeability.md)；本文 `Upgradeability` 列仅记录各 surface 的代码事实快照。

## 2. Surface Map

| Surface | 核心职责 | Authority / Roles | Upgradeability | 证据来源 | 实现状态 |
| --- | --- | --- | --- | --- | --- |
| `src/verse/MemeverseLauncher.sol` | UUPS/core-config facade：UUPS/init/pause、owner + router-hook setter、`removeGasDust`、`setExternalInfo`（governor/registrar ACL）、必需 view getter（含 `claimablePreorderMemecoin`，调 `MemeverseLauncherLib`）、thin delegatecall entry、POLend callback ABI（`settleLeveragedAuxiliaryLiquidity` / `redeemMemecoinLiquidity` 留 facade delegatecall `MemeverseLiquidityImpl`） | owner 配置、UUPS 升级授权；registrar 注册；governor/registrar 元数据更新；业务入口大多 permissionless + stage guard；解锁迁移时为受保护池写入公开 swap 恢复时间 | ERC1967Proxy + UUPS；implementation constructor 锁定 initializers；proxy 通过 `initialize(...)` 写入 registrar、proxy deployer、yield dispatcher、endpoint registry、`POLend`、`POLSplitter` 与初始 gas/reward/preorder 配置；launch / settlement / liquidity 三大业务边界分别经 delegatecall 委托 `MemeverseLaunchImpl` / `MemeverseSettlementImpl` / `MemeverseLiquidityImpl` sibling（owner `setLaunchImpl`/`setSettlementImpl`/`setLiquidityImpl` 替换，共享 ERC-7201 namespace）；fee 预览经独立 `MemeverseFeePreviewReader`（owner `setFeePreviewReader` 替换） | [docs/spec/protocol.md](spec/protocol.md); [docs/spec/verse/state-machines.md](spec/verse/state-machines.md); [docs/spec/upgradeability.md](spec/upgradeability.md); `src/verse/MemeverseLauncher.sol::initialize`、`::_storeInitializedConfig`, `::getStageByVerseId`、`::getYieldVaultByVerseId`、`::getGovernorByVerseId`, `::claimablePreorderMemecoin`, `::setLaunchImpl`, `::setSettlementImpl`, `::setLiquidityImpl`; `script/MemeverseScript.s.sol::_deployMemeverseLauncher`、`::_deployPOLend`、`::_deployPOLSplitter` | 已实现 |
| `src/verse/MemeverseLaunchImpl.sol` | launch 生命周期：`registerMemeverse`、memecoin/POL proxy deploy & initialize、LayerZero peer 配置、`genesis`、`preorder`、`genesisAndPreorder`、`changeStage` stage dispatcher（Genesis→Locked 成功路径内嵌套 delegatecall `MemeverseLiquidityImpl`；Locked→Unlocked 路径内嵌套 delegatecall `MemeverseSettlementImpl`）、治理组件部署编排 | 仅 Launcher facade 经 delegatecall 调用，无 owner、无业务入口 | 非 proxy；owner `setLaunchImpl` 替换；与 Launcher 共享 ERC-7201 namespace `outrun.storage.MemeverseLauncher` 与 `IMemeverseLauncherStorage` | `src/verse/interfaces/IMemeverseLaunchImpl.sol::registerMemeverse`、`::genesis`、`::preorder`、`::genesisAndPreorder`、`::changeStage`; `src/verse/MemeverseLaunchImpl.sol`; `src/verse/MemeverseLauncher.sol::setLaunchImpl`; `src/verse/interfaces/IMemeverseLauncherStorage.sol::MemeverseLauncherStorage` | 已实现 |
| `src/verse/MemeverseSettlementImpl.sol` | settlement / claim / fee 链：`refund` / `refundPreorder` / `claimNormalYT` / `claimNormalFees` / `claimUnlockedPreorderMemecoin` / `redeemAndDistributeFees` / fee 捕获+collect+distribute（含原 `collectAndDistributeFees` + `_captureLockedAuxiliaryFees` 逻辑）、Locked→Unlocked 解算编排（`unlockFromLocked`）、post-unlock 公开 swap 保护 | 仅 Launcher facade 经 delegatecall 调用（含 `changeStage` Locked→Unlocked 嵌套 delegatecall），无 owner、无业务入口 | 非 proxy；owner `setSettlementImpl` 替换；与 Launcher 共享 ERC-7201 namespace `outrun.storage.MemeverseLauncher` 与 `IMemeverseLauncherStorage` | `src/verse/interfaces/IMemeverseSettlementImpl.sol::collectAndDistributeFees`、`::unlockFromLocked`; `src/verse/MemeverseSettlementImpl.sol`; `src/verse/MemeverseLauncher.sol::setSettlementImpl`、`::redeemAndDistributeFees`; `src/verse/interfaces/IMemeverseLauncherStorage.sol::MemeverseLauncherStorage` | 已实现 |
| `src/verse/MemeverseLiquidityImpl.sol` | bootstrap 流动性 / POL mint / LP 赎回：`deployBootstrapLiquidity`（主池+`POL/uAsset`/`PT/uAsset`/`PT/POL` 三辅助池创建、preorder settlement 接线、residual 处置）、`mintPOLToken`、`redeemAuxiliaryLiquidity`、`settleLeveragedAuxiliaryLiquidity`、`redeemMemecoinLiquidity`、LP helper | 仅 Launcher facade 经 delegatecall 调用（含 `changeStage` Genesis→Locked 嵌套 delegatecall），无 owner、无业务入口 | 非 proxy；owner `setLiquidityImpl` 替换；与 Launcher 共享 ERC-7201 namespace `outrun.storage.MemeverseLauncher` 与 `IMemeverseLauncherStorage` | `src/verse/interfaces/IMemeverseLiquidityImpl.sol::deployBootstrapLiquidity`、`::mintPOLToken`; `src/verse/MemeverseLiquidityImpl.sol`; `src/verse/MemeverseLauncher.sol::setLiquidityImpl`、`::mintPOLToken`; `src/verse/interfaces/IMemeverseLauncherStorage.sol::MemeverseLauncherStorage` | 已实现 |
| `src/verse/MemeverseFeePreviewReader.sol` | genesis maker fee 预览与 fee 分发 LayerZero fee 报价。独立 view 合约，通过 immutable `PROXY` staticcall proxy getter 读状态、不绑名域、不被 delegatecall、不改 proxy storage | 无 owner 门禁；任意 caller 可 staticcall；地址替换由 Launcher owner `setFeePreviewReader` 控制 | 非 proxy；owner `setFeePreviewReader` 替换；不共享 Launcher 名域，不可写 proxy storage | `src/verse/interfaces/IMemeverseFeePreviewReader.sol`; `src/verse/MemeverseLauncher.sol::setFeePreviewReader` | 已实现 |
| `src/polend/POLend.sol` | 杠杆创世、债务 cap、PT/YT 结算接线、settlement dust reserve | owner 配置全局利率/杠杆系数/treasury；Launcher 注册、finalize 与 settlement；用户参与杠杆创世和领取 | UUPS proxy | [docs/spec/polend/README.md](spec/polend/README.md); [docs/spec/verse/accounting.md](spec/verse/accounting.md); `src/polend/POLend.sol::onlyLauncher`（modifier）, `::leveragedGenesis` | 已实现 |
| `src/verse/registration/*` + `MemeverseRegistrationCenter` | 注册参数校验、symbol 生命周期、多链 fan-out | center 配置项 onlyOwner；local registrar 仅接受 center 调用；omnichain registrar gas 配置 onlyOwner | 构造部署，不走 proxy | [docs/spec/verse/state-machines.md](spec/verse/state-machines.md); `src/verse/registration/MemeverseRegistrationCenter.sol::registration`, `::setSupportedUAsset`、`::setDurationDaysRange`、`::setRegisterGasLimit`; `src/verse/registration/MemeverseRegistrarAtLocal.sol::localRegistration`、`::registerAtCenter`; `src/verse/registration/MemeverseRegistrarOmnichain.sol::setRegistrationGasLimit` | 已实现 |
| `src/verse/deployment/MemeverseProxyDeployer.sol` | clone/proxy 部署与初始化编排入口 | deploy 函数仅 launcher；`setQuorumNumerator` onlyOwner | 自身不可升级；负责部署可升级与可初始化模块 | [docs/spec/protocol.md](spec/protocol.md); `src/verse/deployment/MemeverseProxyDeployer.sol::_unsafeAllowUninitialized`, `::predictYieldVaultAddress`、`::computeGovernorAndIncentivizerAddress`, `::deployMemecoin`、`::deployPOL`、`::deployYieldVault`、`::deployGovernorAndIncentivizer`, `::setQuorumNumerator` | 已实现 |
| `src/swap/MemeverseSwapRouter.sol` | 用户路由入口（swap/liquidity/fee preview/bootstrap） | swap/liquidity 用户路径 permissionless；fee claim 仅提供 Router 只读 preview helper，执行入口在 Hook core；`createPoolAndAddLiquidity(...)` 为 launcher-only bootstrap；构造固定 `hook/permit2/hookLens` 绑定，并要求 Lens 有代码且 `poolManager` 与 Router 一致；quote/preview 只读路径委托给 `MemeverseUniswapHookLens` | 构造部署，不走 proxy | [docs/spec/protocol.md](spec/protocol.md); [docs/spec/verse/state-machines.md](spec/verse/state-machines.md); `src/swap/MemeverseSwapRouter.sol::beforeDeadline`（modifier）、`::onlyLauncher`（modifier）, `::erc20Pair`（modifier）, `::previewClaimableFees`, `::quoteAmountsForLiquidity`、`::quoteExactAmountsForLiquidity`, `::createPoolAndAddLiquidity`; `src/swap/MemeverseUniswapHookLens.sol`; `src/swap/MemeverseUniswapHook.sol::authorizePoolInitialization` | 已实现 |
| `src/swap/MemeverseYTFlashSwapRouter.sol` | 无独立 YT AMM 的 POL↔YT flash swap Router：两个公开入口（`swapPOLForExactYT` / `swapExactYTForPOL`）、PoolManager unlock callback、真实 `BalanceDelta` 结算、split/merge flash 组合、PT/YT/POL baseline 恢复与重入保护；复用既有 Hook getter `activeAccountSessionPrincipal()` 与 `POLSplitter` split/merge，不复制 fee 数学 | 两入口 permissionless，但任何资金动作前要求 `hook.activeAccountSessionPrincipal() == msg.sender` + 动态校验当前 launcher 的 `getLauncherContracts()` 与 Router immutable 一致；无 Permit2、无 quote 入参、无退款分支 | 非 proxy；构造部署，不走 proxy；immutable 绑定 PoolManager / Hook / POLSplitter | [docs/spec/swap/yt-flash-swap.md](spec/swap/yt-flash-swap.md); [docs/spec/invariants.md INV-24](spec/invariants.md); [docs/spec/access-control.md](spec/access-control.md); `src/swap/MemeverseYTFlashSwapRouter.sol::swapPOLForExactYT`、`::swapExactYTForPOL`、`::_unlockCallback`; `src/swap/interfaces/IMemeverseYTFlashSwapRouter.sol`; `src/swap/MemeverseUniswapHook.sol::activeAccountSessionPrincipal`; `src/polend/POLSplitter.sol::split`、`::merge` | 已实现 |
| `src/swap/MemeverseUniswapHook.sol` | diamond Router：承载 admin/view/liquidity 入口（直接实现）、hook callbacks entry shell、返佣 claim 入口（`claimRebate`）、完整动态费 quote bridge（`quoteSwapFeeWithContext`）与统一 storage struct；callback / fee 分账 / LP per-share accounting / dynamic fee state / preorder settlement logic 经 Router entry `delegatecall` 分发到 SwapFacet / DynamicFeeFacet / SettlementFacet（共享 Router storage） | 核心 API 对外开放（统一在 hook 地址）；fee/treasury/rebate rate 配置 onlyOwner；preorder settlement 与 pool-level 公开 swap 恢复时间写入仅 launcher；公开 swap 保护在 `beforeSwap`（SwapFacet delegatecall）执行；SwapFacet 的 rebate 路径：beforeSwap 主路径（`knownLpInputFee > 0 && knownProtocolInputFee > 0 && effectiveSupply != 0`）走 `_computeRebate` + `_settleProtocolFee` + 合并 take（LP fee + rebate 同币种同收款人合并，省一次 PoolManager take + 一次 ERC20 transfer），beforeSwap 边界（lpFee==0、protocolFee==0、或 effectiveSupply==0（drained pool））+ afterSwap 3 点走 `_collectProtocolFee`（内含 `_computeRebate` + `_settleProtocolFee` + 独立 rebate take）按 referrer 切 rebate，内联累加 `pendingRebate` 并 emit（不经 DynamicFeeFacet）；业务身份检查由 Router 层 modifier（`onlyPoolManager`/`onlyOwner`/`onlyLauncher`/`nonReentrant`/`erc20Pair`/`initializer`）执行，facet logic 仅挂 `onlyViaRouter` 守卫（`address(this)!=__self` 防 direct CALL，`__self` 为 facet 自身地址 immutable，见各 facet 行） | ERC1967Proxy + UUPS；implementation constructor 仅经 ImmutableState 固定 poolManager 并锁定 initializers；proxy 通过 `initialize(initialOwner, treasury_, lpTokenImplementation_, swapFacet_, dynamicFeeFacet_, settlementFacet_, launcher_)` 传入 facet 地址、`launcher` 与其他配置（签名顺序以 [docs/spec/upgradeability.md](spec/upgradeability.md) §2 为真源）；Hook implementation 持 `_authorizeUpgrade(...) => onlyOwner` 升级入口，Hook proxy 升级由 Hook `owner()` 授权；3 facet 地址存 Router storage，`setFacet(role, addr)` onlyOwner 独立升级每个 facet；poolManager 一致性（3 facet 部署时必须传与 hook 同一个 `poolManager`）由 Hook on-chain guard `_requireFacetPoolManager` 在 `initialize`（3 facet）与 `setFacet`（被替换 facet）强制（不匹配 revert `FacetPoolManagerMismatch`），同时也是 operator/off-chain upgrade checklist/runbook 的核验项；此外 readiness（`_requireSwapReady`）额外校验 `router.poolManager() == hook.poolManager()`（错误串 `ROUTER_POOL_MANAGER_NOT_READY`），覆盖 `_requireFacetPoolManager` 未涉及的 router↔hook 对角不变量（Router 在自身 PoolManager 上 unlock/initialize，该 PoolManager 回调 Hook 的 `onlyPoolManager` 回调比对 Hook 的 poolManager immutable，不一致则 `NotPoolManager` DoS）；部署顺序为 3 facet → hook impl → hook proxy（ERC1967Proxy）→ `initialize` 传 facet 地址 | [docs/spec/protocol.md](spec/protocol.md); [docs/spec/verse/state-machines.md](spec/verse/state-machines.md); `src/swap/MemeverseUniswapHook.sol::initialize`, `::setFacet`, `::poolInfo`, `::addLiquidityCore`、`::removeLiquidityCore`、`::claimFeesCore`, `::claimRebate`, `::setReferrerRebateBps`, `::executePreorderSettlement`, `::quoteSwapFeeWithContext`, `::beginAccountSession`, `::endAccountSession`（后两者直接跑 hook 自有 transient store，非 Router `onlyViaRouter` facet 路径），`::activeAccountSessionPrincipal`（只读 transient-principal getter,不改 session 生命周期,见 [docs/spec/swap/yt-flash-swap.md §12.1](spec/swap/yt-flash-swap.md)）; `src/swap/SwapFacet.sol`, `src/swap/DynamicFeeFacet.sol`, `src/swap/SettlementFacet.sol`; `script/DeployMemeverseHookProxy.s.sol` | 已实现 |
| `src/swap/SwapFacet.sol` | hook callback logic + fee 分账胶水 + LP per-share accounting：`beforeSwapLogic`（入口 acquire per-pool transient lifecycle lock；原始请求一次选费、`OrdinarySwapMath` 四路径目标与容量检查）/`afterSwapLogic`（以实际核心 `BalanceDelta` 完整成交、最终用户金额和动态历史后出口 release）/`beforeInitializeLogic`/`beforeAddLiquidityLogic`、partial-fill guard/referrer rebate、`_collectLpFee`/transient context 编码/`_decodeReferrer`（fee helper `_accrueLpFee`/`_resolveSwapFeeContext`/`_isProtocolFeeCurrencySupported` 经 `MemeverseSwapFeeBase` 继承共享，`SwapFeeContext`/`_actualInputAmount`/`_actualOutputAmount` 经 `SwapFeeMath` library 共享）；热路径 fee quote 经 internal `delegatecall` DynamicFeeFacet，只解码一个 `feeBps` word | 无 owner/无业务入口；logic 函数为 `external` 供 Router entry `delegatecall` 编码调用，开头检查 `address(this) != __self` 防 direct CALL（`__self` 为 facet 自身地址 immutable） | 非 proxy；部署时 constructor 传入与 hook 同一个 `poolManager`，DELEGATECALL 下 facet 读取自身 bytecode 中的 immutable；hook owner 经 `setFacet(SWAP_FACET_ROLE, newAddr)` 替换 | `src/swap/SwapFacet.sol::beforeSwapLogic`、`::afterSwapLogic`、`::_prepareSwapFee`、`::beforeInitializeLogic`、`::beforeAddLiquidityLogic`; `src/swap/MemeverseUniswapHook.sol::setFacet`; `src/swap/interfaces/ISwapFacet.sol` | 已实现 |
| `src/swap/DynamicFeeFacet.sol` | dynamic fee state 读写：热路径 `prepareSwapFee` 只返回 `feeBps`；诊断/Lens 路径 `quote` 返回完整静态 11-word `PreparedSwapFee`（含最终用户输入/输出与核心毛输出）；`PrepareSwapFeeParams` 无 launch 字段，launch 配置由 facet 自读共享 ERC7201 storage（`defaultLaunchFeeConfig` / `poolLaunchTimestamp`）；另有 `updateAfterSwap`；EWVWAP/volatility/short-impact 的 pure/view 算法委托 `src/swap/libraries/DynamicFeeMath.sol`（internal 调用 inline 进 facet 字节码），共享刷新 helper `DynamicFeeMath.refreshVolatilityAnchorAndCarry` 由 `DynamicFeeFacet`（`prepareSwapFee`）与 `SettlementFacet`（`executeSettlementLogic`）共同调用，facet 自身仅保留 storage 写入 helper `_updateVolatilityDeviationAccumulatorAfterSwap`；读写 Router storage 的 `dynamicFeeState`；返佣记账由 `SwapFacet::_settleProtocolFee` 内联执行，`_collectProtocolFee` / beforeSwap 主路径均调用它 | 无 owner/无业务入口；logic 函数为 `external` 供 Router / SwapFacet / SettlementFacet `delegatecall` 调用，开头检查 `address(this) != __self` 防 direct CALL（`__self` 为 facet 自身地址 immutable）；业务身份授权由 Router 层负责 | 非 proxy；部署时 constructor 须传与 hook 同一个 `poolManager`（fee logic 本身不调 PoolManager，但 `initialize`/`setFacet` 经 `_requireFacetPoolManager` 校验其 immutable 一致性）；hook owner 经 `setFacet(DYNAMIC_FEE_FACET_ROLE, newAddr)` 替换 | `src/swap/DynamicFeeFacet.sol::prepareSwapFee`、`::updateAfterSwap`、`::quote`; `src/swap/MemeverseUniswapHook.sol::quoteSwapFeeWithContext`、`::setFacet`; `src/swap/interfaces/IDynamicFeeFacet.sol` | 已实现 |
| `src/swap/libraries/OrdinarySwapMath.sol` | 普通动态 Swap 的唯一纯数学归属：由一次选定的 fee 拆分，推导 exact-input/exact-output × 输入/输出侧 protocol fee 四路径的核心目标、最终用户输入/净输出、LP/protocol fee；验证方向敏感 raw price limit，计算 full-range capacity 并拒绝端点耗尽 | 无 owner/无业务入口；纯 internal 调用，无 storage 或外部调用 | 非 proxy；编译期分别内联进 SwapFacet、DynamicFeeFacet 与 Lens bytecode，运行期无独立地址或升级入口；变更时须随调用 surface 更新相应 facet/Lens | `src/swap/libraries/OrdinarySwapMath.sol::deriveSettlementPlan`、`::calculateCapacity`、`::calculateFinalQuoteCurve`、`::deriveFinalSettlement`、`::deriveFeeSplit`、`::validateRawSqrtPriceLimit`、`::calculateOriginalRequestCurve`、`::revertIfFinalTargetIsNotExecutable`; `test/swap/libraries/OrdinarySwapMath.t.sol` | 已实现 |
| `src/swap/libraries/DynamicFeeMath.sol` | dynamic fee pure/view 与刷新算法 library：`quoteLaunchFeeBps`、`selectDynamicFee`、`populateDynamicFeeQuoteFromState`、`decayLinearPpm`、`volatilityRefreshPlan`、`volatilityRefreshApply`、`refreshVolatilityAnchorAndCarry`、`normalizedLaunchDecayWad`、`volatilityDeltaSteps`、`absDiff`；internal 调用 inline 进 facet 字节码，不改变 facet 经 `setFacet(DYNAMIC_FEE_FACET_ROLE, addr)` 替换的升级模型 | 无 owner/无业务入口；internal pure/view 与 storage-writing 函数混合；不独立部署或被 external CALL，全部在调用 facet 字节码中内联执行（DynamicFeeFacet 与 SettlementFacet 均内联） | 非 proxy；internal pure/view/storage-writing 函数在编译期分别内联进 DynamicFeeFacet 与 SettlementFacet bytecode，运行期无独立 library 地址、无 address linkage 或独立升级入口；完整替换 library 实现必须同时通过 `setFacet(DYNAMIC_FEE_FACET_ROLE, newAddr)` 与 `setFacet(SETTLEMENT_FACET_ROLE, newAddr)` 替换两个 facet | `src/swap/libraries/DynamicFeeMath.sol::quoteLaunchFeeBps`、`::selectDynamicFee`、`::populateDynamicFeeQuoteFromState`、`::decayLinearPpm`、`::volatilityRefreshPlan`、`::volatilityRefreshApply`、`::refreshVolatilityAnchorAndCarry`、`::normalizedLaunchDecayWad`、`::volatilityDeltaSteps`、`::absDiff`; `src/swap/DynamicFeeFacet.sol::prepareSwapFee`、`::quote` | 已实现 |
| `src/swap/SettlementFacet.sol` | preorder settlement logic：`executeSettlementLogic`；typed `settlementUnlockCallback`；`UnlockCallbackKind.Settlement` + `SettlementCallbackData` payload，返回 typed `SettlementResult`；swap/settle/take 与固定 settlement fee accounting 均保留在 SettlementFacet | 无 owner/无业务入口；logic 函数为 `external` 供 Router entry `delegatecall` 调用，开头检查 `address(this) != __self` 防直接 CALL（`__self` 为 facet 自身地址 immutable） | 非 proxy；部署时 constructor 传入与 hook 同一个 `poolManager`；hook owner 经 `setFacet(SETTLEMENT_FACET_ROLE, newAddr)` 替换；Router 与 facet 的 typed ABI 必须保持一致；settlement hook self-call 依赖 v4 同时跳过 `beforeSwap` / `afterSwap`，不使用 settlement-specific transient routing state；但接入共享 per-pool swap-lifecycle lock（`MemeverseTransientState.acquireSwapLifecycleLock`/`releaseSwapLifecycleLock`）：`executeSettlementLogic` 在 Phase 1 `transferFrom` 前 acquire、Phase 3 `_updateAfterSwap` 后的函数末尾 release，覆盖 transferFrom + swap + settle/take 全窗口，阻断同池 callback-token reentry（INV-04A）；外部 callback-token 重入 swap 走普通 public callbacks / fee 路径（INV-04A） | `src/swap/SettlementFacet.sol::executeSettlementLogic`; `src/swap/SettlementFacet.sol::settlementUnlockCallback`; `src/swap/MemeverseUniswapHook.sol::unlockCallback`; `src/swap/MemeverseUniswapHook.sol::setFacet`; `src/swap/interfaces/ISettlementFacet.sol`; `src/swap/interfaces/IMemeverseUniswapHook.sol::UnlockCallbackKind` | 已实现 |
| `src/token/Memecoin.sol` + `src/token/MemePol.sol` | OFT memecoin/POL 资产层 | launcher 控制 mint 与 poolId；burn 按持币/授权路径开放 | clone + `initialize` 一次性 | [docs/spec/verse/accounting.md](spec/verse/accounting.md); [docs/spec/access-control.md](spec/access-control.md); `src/token/Memecoin.sol::initialize`、`::mint`、`::burn`; `src/token/MemePol.sol::initialize`、`::setPoolId`、`::mint`; `src/common/access/Initializable.sol::initializer`（modifier）、`::onlyInitializing`（modifier） | 已实现 |
| `src/yield/MemecoinYieldVault.sol` | memecoin yield vault + ERC20Votes share | 无 owner 门禁型业务入口（deposit/yield/redeem）；由流程和资产校验约束 | clone + `initialize` 一次性 | [docs/spec/verse/accounting.md](spec/verse/accounting.md); [docs/spec/access-control.md](spec/access-control.md); `src/yield/MemecoinYieldVault.sol::initialize`, `::accumulateYields`, `::deposit`, `::requestRedeem`, `::executeRedeem`, `::reAccumulateYields` | 已实现 |
| `src/governance/MemecoinDaoGovernorUpgradeable.sol` | DAO Governor + treasury + proposal/vote 扩展 | treasury 支出/升级仅治理执行 | UUPS + initializer | [docs/spec/verse/accounting.md](spec/verse/accounting.md); `src/governance/MemecoinDaoGovernorUpgradeable.sol::__MemecoinDaoGovernor_init`, `::propose`, `::execute`、`::_authorizeUpgrade` | 已实现 |
| `src/governance/GovernanceCycleIncentivizerUpgradeable.sol` | 治理周期奖励与 treasury/reward 账本 | `recordTreasuryIncome` / `recordTreasuryAssetSpend` 仅 governor；`claimReward` 为用户入口；`syncTreasuryBalance` 与 `finalizeCurrentCycle` 可 permissionless | UUPS + initializer | [docs/spec/verse/accounting.md](spec/verse/accounting.md); [docs/spec/access-control.md](spec/access-control.md); `src/governance/GovernanceCycleIncentivizerUpgradeable.sol::__GovernanceCycleIncentivizer_init`、`::onlyGovernance`（modifier）, `::recordTreasuryIncome`, `::recordTreasuryAssetSpend`, `::syncTreasuryBalance`, `::finalizeCurrentCycle`, `::_authorizeUpgrade` | 已实现 |
| `src/verse/YieldDispatcher.sol` | OFT compose 收益分发（governor / yieldVault / burn） | `lzCompose` 仅 endpoint；`distributeSameChain` 仅 launcher；`settlePendingCompose` permissionless | 构造部署，不走 proxy | [docs/spec/protocol.md](spec/protocol.md); [docs/spec/access-control.md](spec/access-control.md); `src/verse/YieldDispatcher.sol::lzCompose`, `::distributeSameChain`, `::settlePendingCompose`, `::_settle`, `::_settleToContract` | 已实现 |
| `src/interoperation/MemeverseOmnichainInteroperation.sol` + `OmnichainMemecoinStaker.sol` | 跨链 staking 入口与治理链落地 | staking permissionless；gas 配置 onlyOwner；staker compose 仅 endpoint；`settlePendingCompose` 仅接收人（receiver == msg.sender） | 构造部署，不走 proxy | [docs/spec/protocol.md](spec/protocol.md); [docs/spec/access-control.md](spec/access-control.md); `src/interoperation/MemeverseOmnichainInteroperation.sol::memecoinStaking`, `::setGasLimits`; `src/interoperation/OmnichainMemecoinStaker.sol::lzCompose`, `::settlePendingCompose` | 已实现 |
| `src/common/omnichain/LzEndpointRegistry.sol` | chainId -> endpointId 注册表 | `setLzEndpointIds` onlyOwner | 构造部署，不走 proxy | [docs/spec/verse/state-machines.md](spec/verse/state-machines.md); `src/common/omnichain/LzEndpointRegistry.sol::setLzEndpointIds` | 已实现 |
| `src/common/omnichain/OFTComposeSettleVerify.sol` | YieldDispatcher / OmnichainMemecoinStaker 共享的 settlePendingCompose 投递+真实性验证库：endpoint composeQueue 读取、NotDelivered / AlreadyExecuted / keccak256 绑定、amountLD + composeMsg 解码（单一事实源，防两 composer 验证前缀漂移） | 无 owner/无业务入口；纯 internal view，无 storage 或外部状态写 | 非 proxy；编译期内联进两 composer 字节码，运行期无独立地址或升级入口 | [docs/spec/interoperation/layerzero-oapp-oft.md](spec/interoperation/layerzero-oapp-oft.md); [docs/spec/invariants.md INV-25](spec/invariants.md); `src/common/omnichain/OFTComposeSettleVerify.sol::verifySettle`; `src/verse/YieldDispatcher.sol::settlePendingCompose`; `src/interoperation/OmnichainMemecoinStaker.sol::settlePendingCompose` | 已实现 |
| `src/common/access/*` + `src/common/omnichain/*`（与本任务相关子集） | 最小代理初始化、owner、peer/delegate 与 OFT/OApp 基础边界 | `initializer` 一次性、`onlyOwner` peer/delegate/msgInspector | 基础能力层 | [docs/spec/access-control.md](spec/access-control.md); `src/common/access/Initializable.sol::initializer`（modifier）、`::onlyInitializing`（modifier）; `src/common/omnichain/oapp/OutrunOAppCoreInit.sol::setPeer`、`::setDelegate`; `src/common/omnichain/oft/OutrunOFTCoreInit.sol::setMsgInspector` | 已实现 |
| `src/credit/GenesisCredit.sol` | GenesisCredit（per-uAsset ERC20+OFT 凭证）：跨链 merkle claim、permissionless 自烧、OFT 桥；支撑 `POLend.leveragedGenesisWithCredit` 的冷启动抵扣与 `finalizeLeveragedGenesis` 的托管 burn | `claim(...)` permissionless（merkle proof 校验，单点写入 home 链防重复领）；`burn(uint256)` permissionless 自烧（POLend 托管余额走该路径）；`setMerkleRoot(...)` owner-only | 非 proxy；由 `GenesisCreditFactory.deployCredit` CREATE3 部署，per-uAsset 本链确定性地址（`salt = keccak256(abi.encode(uAsset))`；跨链同址需 factory+uAsset 均跨链同址，非合约保证） | [docs/spec/polend/genesis.md §4.1](spec/polend/genesis.md); [docs/spec/invariants.md INV-21](spec/invariants.md); `src/credit/GenesisCredit.sol::claim`, `src/credit/GenesisCredit.sol::burn`, `src/credit/GenesisCredit.sol::setMerkleRoot`; `src/credit/interfaces/IGenesisCredit.sol` | 已实现 |
| `src/credit/GenesisCreditFactory.sol` | per-uAsset GenesisCredit CREATE3 部署工厂；提供按 `uAsset` 查 GenesisCredit 地址的 view | `deployCredit(uAsset, name, symbol, delegate)` owner-only，`delegate` 成为对应 GenesisCredit 初始 owner；`creditOf(uAsset)` view 任意地址可读；merkle root 由该 GenesisCredit 的 owner 直接调用 `GenesisCredit.setMerkleRoot(root)` 写入 | 非 proxy；自内联 CREATE3（不依赖 `OutrunDeployer`），仅依赖 LayerZero `lzEndpoint`，无循环依赖（不依赖 POLend） | [docs/spec/polend/genesis.md §4.1](spec/polend/genesis.md); [docs/operations.md](operations.md) GenesisCredit 节; `src/credit/GenesisCreditFactory.sol::deployCredit`, `src/credit/GenesisCreditFactory.sol::creditOf`; `src/credit/GenesisCredit.sol::setMerkleRoot`; `src/credit/interfaces/IGenesisCreditFactory.sol` | 已实现 |

## 2.1 Launcher UUPS 部署事实

- Launcher 使用 `ERC1967Proxy + UUPS`；canonical Launcher address 是 `IOutrunDeployer` CREATE3 部署的 ERC1967 proxy 地址，implementation 只承载逻辑。
- Launcher proxy owner 负责 UUPS 升级授权；脚本使用独立 implementation salt 与 canonical proxy salt，部署后 readiness 校验依赖 back-reference 与 fund metadata。
- `DeployMemeverseHookProxy.DeploymentResult` 返回 6 个 first-class deployment artifact：`hookImplementation`、`hookProxy`、`lpTokenImplementation` 与 3 facet（SwapFacet / DynamicFeeFacet / SettlementFacet），全部参与 same-nonce 复用验证；其中 `hookImplementation`、`lpTokenImplementation` 与各 facet 地址经固定 seed + nonce 预测，必须等于同 nonce 预测地址（`hookProxy` 经 mining 选址，见下条）。
- Hook proxy 使用 `ERC1967Proxy(implementation, initializeData)`（UUPS 不传 owner 给 proxy constructor，owner 经 `initialize` 植入 Hook storage）。same-nonce/existing Hook proxy 复用校验包括 `EXPECTED_HOOK_PROXY_CODEHASH`（ERC1967Proxy runtime）与 Hook `owner()`（完整复用校验路径见 [operations.md](operations.md)）；operational ownership transfer 即升级授权转移，无 ProxyAdmin 对齐需求。
- 3 facet readiness 以运行期 codehash 等于预期值、地址非零且有代码为准；3 facet 部署时必须传与 hook 同一个 `poolManager`——`initialize`/`setFacet` 经 `_requireFacetPoolManager` 对 3 facet 强制一致性（不匹配 revert `FacetPoolManagerMismatch`），SwapFacet/SettlementFacet 另在 DELEGATECALL callback 中读自己 bytecode immutable 调 PoolManager；以及 router↔hook PoolManager 一致性（readiness `_requireSwapReady` 校验 `router.poolManager() == hook.poolManager()`，错误串 `ROUTER_POOL_MANAGER_NOT_READY`）。

## 3. 测试与 Harness 映射状态

- 当前 `.harness/policy.json` 已覆盖：
  - swap / launcher 主面
  - registration / dispatcher / deployment
  - governance
  - interoperation
  - token / yield
  - common 的已测子集（registry、token initializer、OApp initializer、OFT initializer、governance token extension）
- 普通动态 Swap 的实现证据：
  - `test/swap/libraries/OrdinarySwapMath.t.sol`：四路径取整、exact-output 反推、方向敏感 raw limit、全范围 capacity 与端点 equality。
  - `test/swap/DynamicFeeFacet.t.sol`：`prepareSwapFee` 的单 word 热路径、`quote` 的 11-word `PreparedSwapFee` 固定 ABI、一次选费与容量一致性。
  - `test/swap/MemeverseUniswapHookIntegration.t.sol`：Lens/bridge/执行跨方向、请求类型和协议费币腿的一致性，以及不足 price limit 的一致拒绝。
  - `test/swap/MemeverseQuoteReadOnlyInvariant.t.sol`、`test/swap/MemeverseTransientState.t.sol`：报价只读与 encoded fee/pre-price/core-target transient context。
  - 现有迁移兼容性基线：`test/swap/MemeverseUniswapHookIntegration.t.sol`、`test/swap/MemeverseSwapRouter.t.sol`、`test/swap/MemeverseSwapRouterPermit2.t.sol` 与 `test/swap/fork/MemeverseSwapFork*.t.sol`。
- 仍保留 residual testing gap：
  - `src/common/**` 中少量仍未被 targeted rule 单独建模的基础层子集（如 `ReentrancyGuard`、`IBurnable`、`OutrunOAppPreCrimeSimulatorInit`、`TokenHelper`）
  - 证据：`.harness/policy.json`
- 结论
  - 当前已映射实现面覆盖主要业务域；Harness 层“规则 -> 测试映射”已覆盖当前列出的表面，残余缺口已收敛到少量 common 基础件。

## 4. 后续业务解释事项（不影响“已实现”状态）

- `src/governance/GovernanceCycleIncentivizerUpgradeable.sol` 采用“Governor 托管真实资产、Incentivizer 维护账本并结算用户 claim”的治理奖励路径；其当前产品解释已由 [docs/spec/verse/accounting.md](spec/verse/accounting.md)、[docs/spec/access-control.md](spec/access-control.md) 与对应测试证据闭环。
- unlock 后的公开 swap 保护当前由 launcher 在解锁时把恢复时间 snapshot 到 hook，hook 在 `beforeSwap` 执行阻断；该能力没有额外生命周期阶段，但已形成行为闭环。
